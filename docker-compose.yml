version: '3.8'

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: a2a_poc
      POSTGRES_USER: a2a_user
      POSTGRES_PASSWORD: a2a_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: